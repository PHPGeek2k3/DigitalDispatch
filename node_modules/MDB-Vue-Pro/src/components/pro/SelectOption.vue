<template>
  <li :data-multiple="multiple" :class="className">
    <input v-if="multiple" :data-multiple="multiple" type="checkbox" class="form-check-input" :class="{'filled-in': filled}"/>
    <label v-if="multiple" :data-multiple="multiple" :data-value="value" class="form-check-label"></label>
    <img v-if="icon" :src="icon" :data-value="value" :alt="alt" class="rounded-circle" />
    <span :data-multiple="multiple" class="filtrable" :data-value="value">
      <slot></slot>
    </span>
  </li>
</template>

<script>
import classNames from 'classnames';

export default {
  name: 'SelectOption',
  props: {
    multiple: {
      type: Boolean,
      default: false
    },
    disabled: {
      type: Boolean,
      default: false
    },
    value: {
      type: String
    },
    icon: {
      type: String
    },
    alt: {
      type: String,
      default: "Option icon"
    },
    filled: {
      type: Boolean,
      default: false
    }
  },
  computed: {
    className() {
      return classNames(
        this.disabled && 'disabled'
      );
    }
  }
};
</script>

<style scoped>
label {
  float: left;
  margin-left: 10px;
  margin-top: 6px;
  margin-bottom: 0;
}
</style>
