'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = {
    events: {
        'vue-datatables:action': function vueDatatablesAction(action, row, index) {
            var func = action + 'Action';

            if (typeof this[func] === 'function') {
                this[func](row, index);
            }
        }
    },

    methods: {
        /**
         * Initialize DataTables.
         */
        initDataTable: function initDataTable() {
            this.$broadcast('vue-datatables:init');
        },


        /**
         * Refresh DataTables.
         */
        refreshDataTable: function refreshDataTable() {
            this.$broadcast('vue-datatables:refresh');
        }
    }
};